# Explanation: Architecture Overview

## The Compute Continuum
`wasm4pm` operates on a strict doctrine: **Authority lives in Rust. The TypeScript layer is a facade.**

### WASM Boundary
All 41 process mining algorithms and 9 cognition breeds compile to WebAssembly. The execution is bounded by 5 profiles to guarantee deterministic performance across edge devices and massive cloud clusters.

### Adversarial Admissibility
To prevent hallucinations and enforce process realities, every execution passes through 24 adversarial probes. A run is only admitted if it satisfies absolute conformance to the event log mathematics.
