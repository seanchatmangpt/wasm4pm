{#- One row per compat:WorkspaceCrate -> one stub file. Deliberately NOT
    output to <crateDir>/README.md: this repo's crates already carry
    hand-written READMEs (or intentionally don't); this pack never
    overwrites either. output_file uses crateDir as a path segment under
    docs/crates/, safe to regenerate on every ggen sync. #}
# {{ packageName }}

`{{ crateDir }}`

{{ description }}

![wasm-bindgen](https://img.shields.io/badge/wasm--bindgen-{% if hasWasmBindgen == "true" %}yes-success{% else %}no-lightgrey{% endif %})
![tests](https://img.shields.io/badge/tests-{% if hasTests == "true" %}present-success{% else %}absent-red{% endif %})

> Generated by `workspace-pack` from `compat:WorkspaceCrate` facts in
> `packs/workspace-pack/ontology.ttl`. This is a header STUB, not this
> crate's actual `{{ crateDir }}/README.md` — paste the block above into
> the real README by hand if wanted; this file is never the source of
> truth and is safe to regenerate.
