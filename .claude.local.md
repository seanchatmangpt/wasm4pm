# Local Notes (gitignored)

## Session recovery
- Context compaction may lose tool results — re-read files rather than relying on summary quotes
- Memory system: check MEMORY.md index before creating new memory files to avoid duplicates
