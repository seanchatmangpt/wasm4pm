// GENERATED by ggen sync (TICKET-031). Do not hand-edit -- edit the ontology or this template.
export interface Diagnostic {
  message: string;
  severity: "error" | "warning" | "info";
  line?: number;
}

export interface DiagnosticsPanelProps {
  diagnostics: Diagnostic[];
}

export function DiagnosticsPanel({ diagnostics }: DiagnosticsPanelProps) {
  return (
    <ul aria-label="Diagnostics" data-testid="diagnostics-panel">
      {diagnostics.map((d, i) => (
        <li key={i} data-severity={d.severity}>
          {d.line !== undefined ? `L${d.line}: ` : ""}
          {d.message}
        </li>
      ))}
    </ul>
  );
}
