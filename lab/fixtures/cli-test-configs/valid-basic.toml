# Valid basic configuration for wasm4pm testing

[source]
type = "xes"
path = "./test-data/sample.xes"

[discovery]
algorithm = "dfg"
timeout = 30000

[output]
format = "pnml"
path = "./results/model.pnml"
