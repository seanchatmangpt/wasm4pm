{#- Single-file rule: output_file has no {{ }} placeholder, so this
    template receives ALL rows via the `sparql_results` binding -- the real
    convention confirmed against wasm4pm-compat/ggen/templates-breeds/
    registry.json.tera and breed-registration.rs.tera, both of which loop
    `sparql_results` for a whole-result-set, non-per-row render. #}
# wasm4pm Workspace Crate Registry

Generated by `workspace-pack` from `compat:WorkspaceCrate` facts in
`packs/workspace-pack/ontology.ttl`. Do not hand-edit — edit the ontology
and re-run `ggen sync`.

| Crate dir | Package | wasm-bindgen | tests | Description |
|---|---|---|---|---|
{%- for row in sparql_results %}
| `{{ row.crateDir }}` | `{{ row.packageName }}` | {% if row.hasWasmBindgen == "true" %}yes{% else %}no{% endif %} | {% if row.hasTests == "true" %}yes{% else %}no{% endif %} | {{ row.description }} |
{%- endfor %}

Total crates: {{ sparql_results | length }}
