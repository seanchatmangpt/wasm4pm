// GENERATED by ggen sync (TICKET-032). Do not hand-edit -- edit the ontology or this template.
export interface ReplayFailurePresentationProps {
  originalHash: string;
  replayedHash: string;
}

/** Renders an explicit tamper/mismatch state (never a silent wrong value) when a replayed
 * session's final receipt hash diverges from the original -- Architecture Decision 12. */
export function ReplayFailurePresentation({ originalHash, replayedHash }: ReplayFailurePresentationProps) {
  const matches = originalHash === replayedHash;
  return (
    <div role="alert" data-testid="replay-failure-presentation" data-matches={matches}>
      {matches ? (
        <p data-testid="replay-match">Replay verified: hashes match.</p>
      ) : (
        <>
          <strong>Tamper detected: replay hash mismatch.</strong>
          <p data-testid="replay-original-hash">original: {originalHash}</p>
          <p data-testid="replay-replayed-hash">replayed: {replayedHash}</p>
        </>
      )}
    </div>
  );
}
