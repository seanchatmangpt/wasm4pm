// GENERATED by ggen sync (TICKET-033). Do not hand-edit -- edit the ontology or this template.
import type { AccessibilityDefaults } from "../lib/accessibility/defaults";

export interface AccessibilityControlsProps {
  settings: AccessibilityDefaults;
  onChange: (key: keyof AccessibilityDefaults, value: boolean) => void;
}

/** One toggle per AccessibilityDefaults key (16, RDF-derived) -- no accessibility-feature list
 * hardcoded here beyond iterating the typed settings object's own keys. */
export function AccessibilityControls({ settings, onChange }: AccessibilityControlsProps) {
  const keys = Object.keys(settings) as (keyof AccessibilityDefaults)[];
  return (
    <fieldset aria-label="Accessibility settings" data-testid="accessibility-controls" data-control-count={keys.length}>
      {keys.map((key) => (
        <label key={key} data-testid={`accessibility-control-${key}`}>
          <input
            type="checkbox"
            checked={settings[key]}
            onChange={(e) => onChange(key, e.target.checked)}
          />
          {key}
        </label>
      ))}
    </fieldset>
  );
}
