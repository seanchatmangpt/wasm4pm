{
  "name": "@wasm4pm/agent-context",
  "version": "26.6.25",
  "description": "Law 9 organ — injects admitted substrate into agent roles via BLAKE3-anchored snapshots",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "@wasm4pm/contracts": "workspace:*"
  },
  "devDependencies": {
    "blake3": "^2.1.4",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
