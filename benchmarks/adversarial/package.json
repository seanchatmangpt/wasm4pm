{
  "name": "wasm4pm-adversarial-audit",
  "version": "1.0.0",
  "type": "module",
  "description": "Adversarial van der Aalst algorithm audit system",
  "scripts": {
    "build": "tsc",
    "audit": "npm run build && node dist/audit-runner-main.js",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@types/node": "^20.0.0"
  }
}
