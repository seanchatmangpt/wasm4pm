// GENERATED by ggen sync (TICKET-032). Do not hand-edit -- edit the ontology or this template.
import type { RefusalCode } from "../lib/domain/refusal";

export interface RefusalPresentationProps {
  code: RefusalCode;
  reason?: string;
}

/** Displays a refusal. The RefusalCode union (from refusal.ts, RDF-sourced) is the exhaustive,
 * type-checked set of valid values -- no refusal message text is duplicated here beyond a
 * mechanical ALL_CAPS -> Title Case display transform of the code itself. */
export function RefusalPresentation({ code, reason }: RefusalPresentationProps) {
  const display = code
    .toLowerCase()
    .split("_")
    .filter((w) => w.length > 0)
    .map((w) => w.charAt(0).toUpperCase() + w.slice(1))
    .join(" ");
  return (
    <div role="alert" data-testid="refusal-presentation" data-code={code}>
      <strong>{display}</strong>
      {reason && <p>{reason}</p>}
    </div>
  );
}
