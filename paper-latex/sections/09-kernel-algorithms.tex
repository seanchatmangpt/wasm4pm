This section lists the 60 verified kernel algorithms. The full mathematical receipts are provided in Appendix A.

\begin{itemize}
  \item 001 — algorithm: `a\_star` (a\_star)
  \item 002 — algorithm: `aco` (aco)
  \item 003 — algorithm: `alpha\_plus\_plus` (alpha\_plus\_plus)
  \item 004 — algorithm: `declare` (declare)
  \item 005 — algorithm: `dfg` (dfg)
  \item 006 — algorithm: `genetic\_algorithm` (genetic\_algorithm)
  \item 007 — algorithm: `heuristic\_miner` (heuristic\_miner)
  \item 008 — algorithm: `hill\_climbing` (hill\_climbing)
  \item 009 — algorithm: `ilp` (ilp)
  \item 010 — algorithm: `inductive\_miner` (inductive\_miner)
  \item 011 — algorithm: `optimized\_dfg` (optimized\_dfg)
  \item 012 — algorithm: `process\_skeleton` (process\_skeleton)
  \item 013 — algorithm: `pso` (pso)
  \item 014 — algorithm: `simulated\_annealing` (simulated\_annealing)
  \item 015 — algorithm: `hierarchical\_dfg` (hierarchical\_dfg)
  \item 016 — algorithm: `simd\_streaming\_dfg` (simd\_streaming\_dfg)
  \item 017 — algorithm: `smart\_engine` (smart\_engine)
  \item 018 — algorithm: `streaming\_log` (streaming\_log)
  \item 019 — algorithm: `analyze\_process\_speedup` (analyze\_process\_speedup)
  \item 020 — algorithm: `analyze\_variant\_complexity` (analyze\_variant\_complexity)
  \item 021 — algorithm: `batches` (batches)
  \item 022 — algorithm: `causal\_graph` (causal\_graph)
  \item 023 — algorithm: `compute\_activity\_transition\_matrix` (compute\_activity\_transition\_matrix)
  \item 024 — algorithm: `compute\_trace\_similarity\_matrix` (compute\_trace\_similarity\_matrix)
  \item 025 — algorithm: `correlation\_miner` (correlation\_miner)
  \item 026 — algorithm: `log\_to\_trie` (log\_to\_trie)
  \item 027 — algorithm: `performance\_spectrum` (performance\_spectrum)
  \item 028 — algorithm: `transition\_system` (transition\_system)
  \item 029 — algorithm: `alignments` (alignments)
  \item 030 — algorithm: `complexity\_metrics` (complexity\_metrics)
  \item 031 — algorithm: `etconformance\_precision` (etconformance\_precision)
  \item 032 — algorithm: `generalization` (generalization)
  \item 033 — algorithm: `monte\_carlo\_simulation` (monte\_carlo\_simulation)
  \item 034 — algorithm: `playout` (playout)
  \item 035 — algorithm: `bpmn\_import` (bpmn\_import)
  \item 036 — algorithm: `pnml\_import` (pnml\_import)
  \item 037 — algorithm: `powl\_to\_process\_tree` (powl\_to\_process\_tree)
  \item 038 — algorithm: `yawl\_export` (yawl\_export)
  \item 039 — algorithm: `ocel\_dfg` (ocel\_dfg)
  \item 040 — algorithm: `ocel\_dfg\_per\_type` (ocel\_dfg\_per\_type)
  \item 041 — algorithm: `ocel\_encode` (ocel\_encode)
  \item 042 — algorithm: `ocel\_oc\_declare` (ocel\_oc\_declare)
  \item 043 — algorithm: `ocel\_ocla` (ocel\_ocla)
  \item 044 — algorithm: `ocel\_petri\_net` (ocel\_petri\_net)
  \item 045 — algorithm: `compute\_ewma` (compute\_ewma)
  \item 046 — algorithm: `detect\_drift` (detect\_drift)
  \item 047 — algorithm: `predict\_next\_activity` (predict\_next\_activity)
  \item 048 — algorithm: `predict\_outcome` (predict\_outcome)
  \item 049 — algorithm: `predict\_remaining\_time` (predict\_remaining\_time)
  \item 050 — algorithm: `automl\_classify` (automl\_classify)
  \item 051 — algorithm: `automl\_forecast` (automl\_forecast)
  \item 052 — algorithm: `ml\_anomaly` (ml\_anomaly)
  \item 053 — algorithm: `ml\_classify` (ml\_classify)
  \item 054 — algorithm: `ml\_cluster` (ml\_cluster)
  \item 055 — algorithm: `ml\_forecast` (ml\_forecast)
  \item 056 — algorithm: `ml\_pca` (ml\_pca)
  \item 057 — algorithm: `ml\_regress` (ml\_regress)
  \item 058 — algorithm: `handover\_network` (handover\_network)
  \item 059 — algorithm: `working\_together\_network` (working\_together\_network)
  \item 060 — algorithm: `agentic\_pipeline` (agentic\_pipeline)
\end{itemize}
