// GENERATED by ggen sync (TICKET-030). Do not hand-edit -- edit the ontology or this template.
import type { ProblemState } from "../lib/domain/problem-state";

export interface ProblemPanelProps {
  problem: ProblemState;
}

/** Every rendered field is a key of ProblemState (RDF-derived) -- no problem-field label is
 * hardcoded here beyond iterating the typed object's own keys. */
export function ProblemPanel({ problem }: ProblemPanelProps) {
  const entries = Object.entries(problem).filter(([, v]) => v !== undefined);
  return (
    <section aria-label="Problem" data-testid="problem-panel">
      {entries.length === 0 ? (
        <p data-testid="problem-panel-empty">No problem data yet.</p>
      ) : (
        <dl>
          {entries.map(([key, value]) => (
            <div key={key}>
              <dt>{key}</dt>
              <dd>{String(value)}</dd>
            </div>
          ))}
        </dl>
      )}
    </section>
  );
}
