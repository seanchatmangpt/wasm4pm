"""DSPy breed-selection compiler for wasm4pm's cognition kernel.

SELECT (DSPy, optional) -> ADMIT (deterministic, always) -> RUN (real `wpm lab
cognition run`). See README.md for the full calculus and
``~/.claude/plans/review-the-branches-especially-imperative-puppy.md`` for the
design this package implements.
"""

from __future__ import annotations

__all__: list[str] = []
