// GENERATED by ggen sync (TICKET-032). Do not hand-edit -- edit the ontology or this template.
import type { VerificationState } from "../lib/domain/verification-state";

export interface TestResultViewProps {
  verification: VerificationState;
}

export function TestResultView({ verification }: TestResultViewProps) {
  const entries = Object.entries(verification).filter(([, v]) => v !== undefined);
  return (
    <section aria-label="Test results" data-testid="test-result-view">
      {entries.length === 0 ? (
        <p data-testid="test-result-empty">No test results yet.</p>
      ) : (
        <ul>
          {entries.map(([key, value]) => (
            <li key={key} data-testid={`test-result-${key}`}>
              {key}: {String(value)}
            </li>
          ))}
        </ul>
      )}
    </section>
  );
}
